global:
  scrape_interval: 15s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: [otel-collector:8889]

# NOTE: Retention is set via command-line flags on the Prometheus container:
#   --storage.tsdb.retention.time=30d
#   --storage.tsdb.retention.size=10GB
